/** Contracts / completions suggested before next rank gate (presentation). */
export const ALMOST_THERE_CONFIG = {
  /** Main-story contracts weighted toward rank promotion copy. */
  CONTRACTS_FOR_RANK_HINT: 2,
  /** Minimum level gap before showing "Reach Rank X" as primary objective. */
  MIN_LEVEL_FOR_RANK_OBJECTIVE: 3,
} as const
