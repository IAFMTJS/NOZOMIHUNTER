/** Corrupted JP token detection — re-export for narrative/dungeon paths. */
export {
  corruptDisplayToken,
  isCorruptedJapanese,
  isCorruptedRomaji,
  validateLearnerToken,
  type CorruptedTokenCheck,
} from "@/systems/retention/corruptedLanguageSystem"
